# Architecture Decision Records

This directory holds ADRs written by Gyre SE's Phase 1.5. Each file is
`NNNN-<slug>.md` where NNNN is a zero-padded sequence number. See
`.github/gyre-se-conventions.md` for the template.

ADRs are append-only once accepted. Supersede rather than rewrite.
