# Tasks: Issue #3709 - Match multi-token `tau-tui` command palette queries across command metadata

- [x] T1. Write RED tests for multi-token metadata-aware command-palette
      matching.
- [x] T2. Add multi-token metadata matching without regressing existing ranking
      and explicit scaffold handling.
- [x] T3. Verify multi-token metadata matching and confirm earlier M335 REPL
      tests still pass.
