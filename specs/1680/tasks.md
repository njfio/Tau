# Issue 1680 Tasks

Status: In Progress

## Ordered Tasks

T1 (tests-first): capture RED by executing missing split-conformance harness.

T2: add issue artifacts and create
`scripts/dev/test-github-issues-runtime-domain-split.sh`.

T3: run GREEN harness and targeted runtime tests.

T4: run roadmap/fmt/clippy checks and prepare PR closure evidence.

## Tier Mapping

- Functional: runtime split harness (line budget + module/file markers)
- Regression: targeted GitHub Issues runtime tests
- Integration: issue verification command sequence
