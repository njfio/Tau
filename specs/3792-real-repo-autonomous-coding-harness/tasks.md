# Tasks: Real-repo autonomous coding harness

- [x] T1: Add red tests for multi-file mission run, outside-repo rejection, provider `edits` parsing, and multi-file resume.
  - RED: `CARGO_TARGET_DIR=/tmp/rust_pi-3792-red-target cargo test -p tau-agent-core coding_mission -- --test-threads=1` failed before implementation because `CodingMissionRunRequest` and `CodingMissionResumeRequest` had no `controlled_edits` field.
  - RED: `CARGO_TARGET_DIR=/tmp/rust_pi-3792-red-target cargo test -p tau-coding-agent --bin tau_live_coding_loop_harness provider_backed -- --test-threads=1` failed before implementation because `ProviderEditResolution` had no `edits` field.
- [x] T2: Implement backward-compatible multi-edit support in `CodingMissionRunner`.
  - Added serde-default `controlled_edits` vectors while preserving legacy `controlled_edit`.
  - Added edit-set normalization, full target prevalidation, multi-file checkpoint fingerprinting, and blocked fail-closed handling for escaped paths.
- [x] T3: Extend provider payload parsing and harness request construction for multi-file edit plans.
  - Provider JSON now accepts either legacy single-edit shape or `{"edits":[...]}`.
  - Provider resume path passes the edit vector into `CodingMissionRunner`.
- [x] T4: Add deterministic real-repo/worktree harness coverage with PR-ready evidence.
  - Added `tau_live_coding_loop_harness --mode real-repo` with explicit verifier commands and real worktree base-branch detection.
  - Added `scripts/dev/test-real-repo-autonomous-coding-harness.sh`.
- [x] T5: Run scoped unit, integration, formatting, and lint validation; update docs/status truthfully.
  - GREEN: `cargo fmt --check`
  - GREEN: `CARGO_TARGET_DIR=/tmp/rust_pi-3792-red-target cargo test -p tau-agent-core coding_mission -- --test-threads=1` passed 27 mission tests.
  - GREEN: `CARGO_TARGET_DIR=/tmp/rust_pi-3792-red-target cargo test -p tau-coding-agent --bin tau_live_coding_loop_harness provider_backed -- --test-threads=1` passed 5 provider parser tests.
  - GREEN: `CARGO_TARGET_DIR=/tmp/rust_pi-3792-red-target ./scripts/dev/test-full-autonomous-coding-loop.sh`
  - GREEN: `CARGO_TARGET_DIR=/tmp/rust_pi-3792-red-target ./scripts/dev/test-real-repo-autonomous-coding-harness.sh`
  - GREEN: `CARGO_TARGET_DIR=/tmp/rust_pi-3792-red-target cargo clippy -p tau-agent-core --lib --tests -- -D warnings`
  - GREEN: `CARGO_TARGET_DIR=/tmp/rust_pi-3792-red-target cargo clippy -p tau-coding-agent --bin tau_live_coding_loop_harness -- -D warnings`
  - RED: `RUST_MIN_STACK=8388608 CARGO_TARGET_DIR=/tmp/rust_pi-3792-red-target cargo test -p tau-coding-agent --bin tau_live_coding_loop_harness provider_backed_openrouter_api_key_mode_injects_openrouter_key -- --nocapture` failed before the provider proof auth fix because `provider_cli` left `cli.openai_api_key` unset when only `OPENROUTER_API_KEY` supplied the OpenRouter credential.
  - GREEN: `RUST_MIN_STACK=8388608 CARGO_TARGET_DIR=/tmp/rust_pi-3792-red-target cargo test -p tau-coding-agent --bin tau_live_coding_loop_harness provider_backed -- --nocapture` passed 6 provider-backed harness tests.
  - GREEN: `CARGO_TARGET_DIR=/tmp/rust_pi-3792-red-target cargo clippy -p tau-coding-agent --bin tau_live_coding_loop_harness -- -D warnings`
  - LIVE GREEN: `TAU_LIVE_PROVIDER_PROOF=1 TAU_PROVIDER_PROOF_MODEL=openrouter/deepseek/deepseek-v4-flash TAU_PROVIDER_PROOF_REPORT_DIR=/tmp/tau-3792-provider-live-fixed CARGO_TARGET_DIR=/tmp/rust_pi-3792-red-target ./scripts/dev/test-provider-backed-autonomous-coding-loop.sh` passed against OpenRouter/DeepSeek. Sanitized report: `passed=true`, `phase=pr_ready`, provider `openrouter`, model `deepseek/deepseek-v4-flash`, parsed edit path `status.txt`, response bytes `89`, response SHA-256 `cf341f4f557518800b7a0802d3e35f61d024dbcf144aa34855c07610843ef607`.
- [x] T6: Harden direct multi-file runtime reliability and provider response recovery.
  - Added direct `write_many` builtin tool registration so prompt-mode agents can create a complete multi-file edit set in one checked batch.
  - `write_many` validates every target, duplicate path, protected path, RBAC, approval, and rate-limit gate before writing any file.
  - Provider-backed harness now sends real repo status/file snippets, asks for a complete edit set in one response, accepts compact `{"files":{...}}` payloads, and extracts valid JSON from fenced/prose-wrapped provider responses.
  - GREEN: `CARGO_TARGET_DIR=/tmp/rust_pi-write-many-target cargo test -p tau-tools write_many -- --test-threads=1` passed 4 tests covering registration, multi-file write, duplicate rejection, and escape rejection.
  - GREEN: `CARGO_TARGET_DIR=/tmp/rust_pi-harness-provider-target cargo test -p tau-coding-agent --bin tau_live_coding_loop_harness provider_backed -- --test-threads=1` passed 11 provider-backed harness tests.
  - GREEN: `CARGO_TARGET_DIR=/tmp/rust_pi-prompt-write-many-target cargo test -p tau-coding-agent integration_run_prompt_with_cancellation_promotes_textual_write_many_tool_call_and_persists_files -- --test-threads=1` passed prompt-loop batch-tool execution.
  - LIVE GREEN: `TAU_LIVE_PROVIDER_PROOF=1 TAU_PROVIDER_PROOF_REPORT_DIR=/tmp/tau-qwen-provider-live CARGO_TARGET_DIR=/tmp/rust_pi-harness-provider-target ./scripts/dev/test-provider-backed-autonomous-coding-loop.sh` passed against OpenRouter/Qwen. Sanitized report: `passed=true`, `phase=pr_ready`, provider `openrouter`, model `qwen/qwen3.7-max`, parsed edit path `status.txt`, response bytes `64`, response SHA-256 `96567793c59f3f3c74037dd3fe395481b5c56300e7aaa592411747be7585bd24`.
  - LIVE GREEN: `TAU_LIVE_PROVIDER_REPAIR_PROOF=1 TAU_PROVIDER_PROOF_REPORT_DIR=/tmp/tau-qwen-provider-repair CARGO_TARGET_DIR=/tmp/rust_pi-harness-provider-target ./scripts/dev/test-provider-verifier-repair-loop.sh` passed against OpenRouter/Qwen. Sanitized live report: `passed=true`, `phase=pr_ready`, provider attempts `2`, live repair attempt `repair_context_included=true`, `failed_verifier_count=3`, edit paths `notes.txt,status.txt`, response bytes `111`, response SHA-256 `c4e02e0697828ca882d24109801d659e4293dea126ee3cdd6157502cf9f17d4f`.
  - LIVE GREEN: prompt-mode direct run from `/tmp/tau-qwen-write-many.YZNZba` with `--model openrouter/qwen/qwen3.7-max --prompt ...write_many...` created `index.html` and `src/main.js` in one assistant tool call; session DB recorded assistant `tool_call` name `write_many` and tool result `tool_name=write_many`, `file_count=2`.
- [x] T7: Add first-class direct multi-file surgical patching.
  - Added direct `edit_many` builtin tool registration so prompt-mode agents can patch multiple files without rewriting whole files.
  - `edit_many` accepts ordered `{"path","find","replace","all"}` entries, supports multiple patches to the same file, and prevalidates every path/match/size/policy gate before writing any file.
  - GREEN: `CARGO_TARGET_DIR=/tmp/rust_pi-edit-many-target cargo test -p tau-tools edit_many -- --test-threads=1` passed 5 tests covering registration, multi-file patching, same-file ordered patches, missing-match fail-closed behavior, and escape rejection.
  - GREEN: `CARGO_TARGET_DIR=/tmp/rust_pi-prompt-edit-many-target cargo test -p tau-coding-agent integration_run_prompt_with_cancellation_promotes_textual_edit_many_tool_call_and_patches_files -- --test-threads=1` passed prompt-loop batch patch execution.
  - LIVE GREEN: prompt-mode direct run from `/tmp/tau-qwen-edit-many.ET8Eka` with `--model openrouter/qwen/qwen3.7-max --prompt ...edit_many...` patched existing `index.html` and `src/main.js` in one assistant tool call; session DB recorded assistant `tool_call` name `edit_many` and tool result `tool_name=edit_many`, `edit_count=2`.
- [x] T8: Add unified-diff hunk parsing to direct multi-file patching.
  - Extended `edit_many` to accept exactly one of `edits` or `diff`.
  - Added conservative unified-diff support for existing-file modifications: `---`/`+++` file headers, `@@` hunks, context lines, removed lines, added lines, and `diff --git` metadata boundaries.
  - Unified-diff mode rejects `/dev/null` create/delete patches, path escapes, duplicate file patches, mismatched hunk context/removals, malformed hunk lines, and oversize edited output before writing any file.
  - GREEN: `CARGO_TARGET_DIR=/tmp/rust_pi-edit-many-target cargo test -p tau-tools edit_many -- --test-threads=1` passed 11 tests covering exact edits plus unified-diff multi-file patching, same-file multi-hunk patching, mismatch fail-closed behavior, escape rejection, rename rejection, and malformed hunk header rejection.
  - GREEN: `CARGO_TARGET_DIR=/tmp/rust_pi-prompt-edit-many-target cargo test -p tau-coding-agent integration_run_prompt_with_cancellation_promotes_textual_edit_many_unified_diff_tool_call_and_patches_files -- --test-threads=1` passed prompt-loop unified-diff patch execution.
  - LIVE GREEN: prompt-mode direct run from `/tmp/tau-qwen-edit-many-diff.OJ5afc` with `--model openrouter/qwen/qwen3.7-max --prompt ...edit_many diff...` patched existing `index.html` and `src/main.js`; Qwen performed two read calls first, then one assistant `tool_call` named `edit_many` with a `diff` argument, and the session DB recorded `tool_name=edit_many`, `edit_count=2`.
