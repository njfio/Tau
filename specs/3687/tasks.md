# Tasks: Issue #3687 - Group `tau-tui` command palette commands into operator sections

- [x] T1. Write RED tests for grouped command rendering, filtered section
      visibility, and grouped browsing stability.
- [x] T2. Extend the command catalog with section metadata.
- [x] T3. Render grouped sections in the command palette without breaking
      selection, paging, or preview behavior.
- [x] T4. Verify grouped rendering behavior and confirm earlier M335 REPL tests
      still pass.
