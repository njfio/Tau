# Tasks: Issue #3698 - Add forward word deletion shortcut to `tau-tui` command palette

- [x] T1. Write RED tests for line-start and mid-line next-word deletion in the
      command palette.
- [x] T2. Add the command-palette helper and key routing for `Alt+D`.
- [x] T3. Update palette help text for the forward word deletion shortcut.
- [x] T4. Verify command-palette forward word deletion and confirm earlier M335
      REPL tests still pass.
