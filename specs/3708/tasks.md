# Tasks: Issue #3708 - Normalize separator punctuation in `tau-tui` command palette queries

- [x] T1. Write RED tests for space-vs-hyphen command-palette matching.
- [x] T2. Add separator-normalized query matching without regressing existing
      ranking and explicit scaffold handling.
- [x] T3. Verify separator-normalized matching and confirm earlier M335 REPL
      tests still pass.
