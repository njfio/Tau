# Tasks: Break Read-Only Timeout Spiral In Gateway Action Retries

- [x] T1. Extend the timeout-retry regression to assert compacted retry context and mutation-first guidance.
- [x] T2. Rewrite failed read-only retry context into original-task + observation-summary form.
- [x] T3. Strengthen the retry prompt so mutation-missing retries explicitly require a mutating tool next.
- [x] T4. Run scoped gateway verification and confirm the retry payload shape.
