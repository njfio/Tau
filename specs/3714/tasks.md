# Tasks: Issue #3714 - Surface alias-match provenance in tau-tui command palette preview

- [x] T1. Write RED tests for alias-query preview provenance and canonical-query omission.
- [x] T2. Add alias-match provenance to the command-palette preview without changing matching behavior.
- [x] T3. Verify alias-match provenance and confirm earlier M335 REPL tests still pass.
