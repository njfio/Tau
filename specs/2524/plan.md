# Plan #2524

1. Extend built-in tools with `send_file`.
2. Reuse suppression pathway from skip/react with send_file-specific parser.
3. Extend outbound event payload schema with send-file metadata.
4. Validate with conformance + regression tests.
