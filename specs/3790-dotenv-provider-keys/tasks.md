# Tasks: Repo `.env` provider key fallback

- [x] T1: Write spec-derived tests for `.env` fallback, process-env precedence, and absent file behavior.
- [x] T2: Implement provider key candidate lookup from nearest `.env` without overriding process env.
- [x] T3: Create local ignored `.env` with OpenRouter/DeepSeek V4 provider proof settings.
- [x] T4: Validate unit tests, provider-backed proof, and secret-safe reports.
