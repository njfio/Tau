# Tasks: Force Concrete Mutating Tool Choice On Ralph-Loop Recovery Retries

- [ ] T1. Add/adjust failing gateway regressions for concrete mutation retry tool choice.
- [ ] T2. Implement retry tool-choice selection for create/build/new-folder mutation retries.
- [ ] T3. Preserve fallback to generic `Required` when no safe concrete choice exists.
- [ ] T4. Run scoped gateway verification.
