# Tasks: Force Concrete Mutating Tool Choice On Ralph-Loop Recovery Retries

- [x] T1. Add/adjust failing gateway regressions for concrete mutation retry tool choice.
- [x] T2. Implement retry tool-choice selection for create/build/new-folder mutation retries.
- [x] T3. Preserve fallback to generic `Required` when no safe concrete choice exists.
- [x] T4. Run scoped gateway verification.
