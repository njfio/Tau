# Tasks: Issue #3677 - Raise `tau-tui` to a world-class operator REPL

- [x] T1. Resolve the primary REPL workflow for the first slice and update
      `spec.md` from Draft to Reviewed.
- [x] T2. Write RED tests for `/status`, `/retry`, `/detach`, and runtime
      status rendering in `tau-tui`.
- [x] T3. Implement runtime control and observability in
      `crates/tau-tui/src/interactive`.
- [x] T4. Verify the new workflow and confirm existing gateway/TUI behavior
      still passes.
