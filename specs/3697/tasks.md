# Tasks: Issue #3697 - Add word-wise deletion shortcut to `tau-tui` command palette

- [x] T1. Write RED tests for end-of-line and mid-line previous-word deletion in
      the command palette.
- [x] T2. Add the command-palette helper and key routing for `Ctrl+W`.
- [x] T3. Update palette help text for the word-wise deletion shortcut.
- [x] T4. Verify command-palette word deletion and confirm earlier M335 REPL
      tests still pass.
