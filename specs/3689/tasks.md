# Tasks: Issue #3689 - Add fuzzy command filtering to `tau-tui` command palette

- [x] T1. Write RED tests for sparse fuzzy queries, ordering guarantees, and
      fuzzy-filtered grouped rendering.
- [x] T2. Extend the command matcher with subsequence fallback ranking.
- [x] T3. Keep grouped rendering and selected previews stable under fuzzy
      results.
- [x] T4. Verify fuzzy filtering behavior and confirm earlier M335 REPL tests
      still pass.
