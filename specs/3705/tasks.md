# Tasks: Issue #3705 - Make `Esc` clear active placeholder focus before closing `tau-tui` command palette

- [x] T1. Write RED tests for active-placeholder `Esc` focus clearing and the
      follow-up palette close path.
- [x] T2. Update command-palette `Esc` handling so active placeholder focus
      clears before the ordinary close path runs.
- [x] T3. Verify placeholder-aware `Esc` handling and confirm earlier M335 REPL
      tests still pass.
