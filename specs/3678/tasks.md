# Tasks: Issue #3678 - Add prompt history and editor ergonomics to `tau-tui` REPL

- [x] T1. Write RED tests for prompt history recall, draft preservation, and
      stronger insert-mode editor controls.
- [x] T2. Implement prompt history state and editor helpers in
      `crates/tau-tui/src/interactive`.
- [x] T3. Update help/status surfaces to expose the new editing shortcuts.
- [x] T4. Verify the new editor/history workflow and confirm existing gateway
      TUI behavior still passes.
