# Tasks 3775: Keep Canonical Benchmark Visible In Dashboard Viewport

- [x] T1: Add a conformance test proving the mission list uses a scroll budget
  and the benchmark panel remains a first-viewport anchor.
- [x] T2: Add the mission-list scroll budget, table scroll region, and benchmark
  anchor markers.
- [x] T3: Run focused dashboard tests, full dashboard tests, lint/style checks,
  and browser geometry validation.
