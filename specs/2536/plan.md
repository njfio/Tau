# Plan #2536

1. Add RED tests for profile routing schema parsing and runtime model resolution behavior.
2. Extend profile defaults schema with routing struct and default/backfill compatibility.
3. Implement prompt complexity + task override selection helpers.
4. Wire scoped model override application in runtime prompt execution paths.
5. Run verification gates and update G15 checklist docs.
