# Tasks: Issue #3686 - Add selected-command preview details to `tau-tui` command palette

- [x] T1. Write RED tests for selected-command preview rendering and selection
      tracking across browsing.
- [x] T2. Extend the command catalog with compact preview metadata.
- [x] T3. Render selected-command preview details in the command palette
      without breaking paging and selection behavior.
- [x] T4. Verify selected-command preview behavior and confirm earlier M335 REPL
      tests still pass.
