# Tasks: Issue #3707 - Match `tau-tui` command palette queries against usage and scaffold text

- [x] T1. Write RED tests for usage-token and scaffold-token command-palette
      matching.
- [x] T2. Extend command-palette filtering to consider `usage` and scaffold text
      without regressing existing ranking behavior.
- [x] T3. Verify usage/scaffold-aware filtering and confirm earlier M335 REPL
      tests still pass.
