# Plan #2535

1. Extend profile defaults schema with routing fields and backward-compatible defaults.
2. Add runtime routing policy helpers (complexity scoring + task override resolution).
3. Wire scoped dispatch model overrides for prompt execution paths.
4. Validate with conformance and regression coverage.
