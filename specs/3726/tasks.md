# Tasks: Issue #3726 - Add schema and validator for tranche-one autonomy benchmark contract

- [x] T1. Define the governed schema/validator scope for the benchmark contract.
- [x] T2. Add the tranche-one autonomy benchmark schema under `tasks/schemas/`.
- [x] T3. Add a deterministic validator command under `scripts/dev/`.
- [x] T4. Add focused shell/Python contract tests for the schema and validator.
- [x] T5. Link Issue `#3726` into the M334 milestone issue hierarchy and close
  the slice with verification evidence.
